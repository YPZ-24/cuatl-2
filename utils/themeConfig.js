import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
    palette: {
        primary: {
            main: 'rgb(163,25,91)'
        },
        secondary: {
            main: 'rgb(4,162,156)'
        },
    }
  });

export default theme;